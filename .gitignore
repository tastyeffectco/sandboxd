# Local environment / secrets
.env
*.env.local

# Runtime data (workspaces, SQLite state, logs) — never commit
/data/
*.db
*.db-wal
*.db-shm

# Build artifacts
/control-plane/sandboxd
/control-plane/runtimed
*.test
*.out

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Stray dev/generated files — never commit
*.log
docker-compose.override.yml
/abs-manifest.yaml
/gitea.yaml

# generated console login (plaintext, local only)
.console-login

# internal maintainer docs — kept locally, never published
docs/internal/

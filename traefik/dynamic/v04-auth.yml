# Generated by install-v04-ubuntu.sh — gates the public console with HTTP
# basic auth. Remove this file (and the override) at teardown.
http:
  middlewares:
    v04-auth:
      basicAuth:
        users:
          - "demo:$apr1$OarfQWxo$W8udY71fGbzDdAjpn0gha0"
